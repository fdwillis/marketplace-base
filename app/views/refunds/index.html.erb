<% @refunds.each do |refund| %>
  <%= refund.title %>
  <%= number_to_currency((refund.price / 100), precision: 2) %>
  <%= refund.status %>
  <%= refund.purchase_id %>
  <%= refund.created_at.to_date.strftime("%B #{refund.created_at.to_date.day.ordinalize}, %Y") %>
<% end %>